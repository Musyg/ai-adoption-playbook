import assert from "node:assert/strict";
import test from "node:test";

async function render(pathname = "/") {
  const workerUrl = new URL("../dist/server/index.js", import.meta.url);
  workerUrl.searchParams.set("test", `${process.pid}-${Date.now()}`);
  const { default: worker } = await import(workerUrl.href);
  const request = new Request(`http://localhost${pathname}`, {
    headers: { accept: "text/html" },
  });
  if (typeof worker === "function") return worker(request);
  return worker.fetch(
    request,
    { ASSETS: { fetch: async () => new Response("Not found", { status: 404 }) } },
    { waitUntil() {}, passThroughOnException() {} },
  );
}

for (const [pathname, language, title] of [
  ["/", "en", "AI Adoption Playbook"],
  ["/fr", "fr", "Playbook d’adoption de l’IA"],
]) {
  test(`server-renders the ${language} playbook`, async () => {
    const response = await render(pathname);
    assert.equal(response.status, 200);
    assert.match(response.headers.get("content-type") ?? "", /^text\/html\b/i);

    const html = await response.text();
    const renderedText = html.replaceAll("<!-- -->", "");
    if (language === "fr") assert.match(html, /<div lang="fr">/);
    if (language === "en") assert.match(html, /<html lang="en">/);
    assert.match(html, new RegExp(title, "i"));
    assert.match(html, /Evidence before autonomy|La preuve avant l’autonomie/i);
    assert.match(html, /Atelier Horizon/);
    assert.match(html, /8 min 35/);
    assert.match(html, /Continue conditionally|Continuer sous conditions/i);
    assert.match(html, /id=["']sme-case["']/i);
    assert.match(html, /Noroît Mécanique SA/);
    assert.match(html, /76 → 27 min/);
    assert.match(renderedText, /60[.,]8 h/);
    assert.match(renderedText, /129[.,]2 h/);
    assert.match(html, /163\/238/);
    assert.match(html, /about −45%|environ −45 %/i);
    assert.match(html, /autonomous completion at A2|traitement autonome au niveau A2/i);
    assert.match(html, /Three evidence classes|Trois classes de preuves/i);
    assert.match(html, /sme-b2b-quote-business-agent|pme-agent-metier-devis-b2b/i);
    assert.match(html, /id=["']mission-case["']/i);
    assert.match(html, /Fondation Lien Local/);
    assert.match(html, /96 → 39 min/);
    assert.match(renderedText, /23[.,]8 h/);
    assert.match(renderedText, /53[.,]8 h/);
    assert.match(html, /58\/86/);
    assert.match(html, /≈ −39%|≈ −39 %/i);
    assert.match(html, /funding decisions made by people|décisions de financement prises par des personnes/i);
    assert.match(html, /mission gate has four vetoes|mission impose quatre conditions bloquantes/i);
    assert.match(renderedText, /1% yes|1 % oui/i);
    assert.match(html, /nonprofit-grant-dossier-business-agent|association-agent-dossiers-subventions/i);
    assert.match(html, /id=["']public-case["']/i);
    assert.match(html, /City of Mont-Rive|Ville de Mont-Rive/i);
    assert.match(html, /145 → 58 min/);
    assert.match(renderedText, /39[.,]2 h/);
    assert.match(renderedText, /106[.,]5 h/);
    assert.match(html, /121\/166/);
    assert.match(html, /≈ −35%|≈ −35 %/i);
    assert.match(html, /public decisions made by qualified people|décisions publiques prises par des personnes qualifiées/i);
    assert.match(html, /Six gates before public production|Six décisions avant la production publique/i);
    assert.match(renderedText, /60–80%|60–80 %/i);
    assert.match(renderedText, /18[.,]5 h → 16 min|18\.5h → 16m/i);
    assert.match(html, /low case fails the economic gate|cas bas échoue au seuil économique/i);
    assert.match(html, /public-sector-planning-dossier-business-agent|service-public-agent-dossiers-urbanisme/i);
    assert.match(html, /Camille Rey/);
    assert.match(html, /44 to 34 minutes|44 à 34 minutes/i);
    assert.match(html, /4\/14/);
    assert.match(html, /Extend the draft-only copilot|Prolonger 30 jours/i);
    assert.match(html, /Copilot, business agent|Copilote, agent métier/i);
    assert.match(html, /Why only|Pourquoi seulement/i);
    assert.doesNotMatch(html, /×5–12/);
    assert.match(html, /5[.,]8%|5,8 %/);
    assert.match(html, /16[.,]8%|16,8 %/);
    assert.match(html, /3[.,]2%|3,2 %/);
    assert.match(html, /15[.,]2%|15,2 %/);
    assert.match(html, /full 20-source evidence review|revue complète de 20 sources/i);
    assert.match(html, /field-evidence-review-2026(?:\.fr)?\.md/);
    assert.match(html, /Camille Rey · Phase 2/);
    assert.match(html, /44 → 14 min/);
    assert.match(html, /13\/20/);
    assert.match(html, /Keep A2 for 60 days|Conserver A2 pendant 60 jours/i);
    assert.match(html, /Do not claim A3|Ne pas revendiquer A3/i);
    assert.match(html, /Camille Rey · Standard diagnostic|Camille Rey · Diagnostic standard/i);
    assert.match(html, /7h 40|7 h 40/);
    assert.match(html, /×7[.,]9/);
    assert.match(html, /8\/17/);
    assert.match(html, /A4 remains unproven|A4 reste à démontrer/i);
    assert.match(html, /Talos\/Hermes analogy|Analogie Talos\/Hermes/i);
    assert.match(html, /Measure the task, not the hype|Mesurer la tâche, pas la promesse/i);
    assert.match(html, /Define one task|Définir une seule tâche/i);
    assert.match(html, /TT-2025-ANTHROPIC-MODEL-ESTIMATE/);
    assert.match(renderedText, /33[.,]1%/);
    assert.match(html, /How the estimated saving is calculated|Comment le gain de temps est calculé/i);
    assert.match(html, /Example only · not used in the calculation|Exemple seulement · non utilisé dans le calcul/i);
    assert.match(html, /Pilot preregistered|Pilote préenregistré/i);
    assert.match(renderedText, /≥ 20%/);
    assert.match(html, /REWORK \+ RERUN|CORRIGER ET REJOUER/i);
    assert.match(html, /id=["']evidence-gate["']/i);
    assert.match(html, /GATE LEDGER|REGISTRE DES SEUILS/i);
    assert.match(html, /CONTINUE BOUNDED|CONTINUER DANS LES LIMITES/i);
    assert.match(renderedText, /35[.,]8%/);
    assert.match(renderedText, /20\/20/);
    assert.match(html, /id=["']operations["']/i);
    assert.match(html, /Production is a reversible operating state|La production est un état réversible/i);
    assert.match(html, /LIVE · BOUNDED|ACTIF · BORNÉ/i);
    assert.match(html, /ROLLBACK IN FIVE PROVABLE STEPS|RETOUR ARRIÈRE EN CINQ ÉTAPES VÉRIFIABLES/i);
    assert.match(renderedText, /1 September 2026|1 septembre 2026/i);
    assert.match(html, /id=["']decision-dossier["']/i);
    assert.match(html, /DRAFT DOSSIER|DOSSIER BROUILLON/i);
    assert.match(html, /ATTACH OR REFERENCE THESE SIX RECORDS|JOINDRE OU RÉFÉRENCER CES SIX ENREGISTREMENTS/i);
    assert.match(html, /Download Markdown dossier|Télécharger le dossier Markdown/i);
    assert.match(html, /Guided decision path|Parcours de décision guidé/i);
    assert.match(html, /id=["']guided-start["']/i);
    assert.match(html, /Build your route, one simple choice at a time|Construisez votre parcours, un choix simple à la fois/i);
    assert.match(html, /What kind of organization are you guiding|Quel type d’organisation accompagnez-vous/i);
    assert.match(html, /Why ask this first|Pourquoi commencer ici/i);
    assert.match(html, /id=["']concept-library["']/i);
    assert.match(html, /id=["']operational-workspace["']/i);
    assert.match(html, /id=["']implementation-library["']/i);
    assert.match(html, /Understand the differences|Comprendre les différences/i);
    assert.match(html, /Build and operate a pilot|Construire et exploiter un pilote/i);
    assert.match(html, /Choose what you need now|Choisissez ce dont vous avez besoin maintenant/i);
    assert.match(html, /Only the selected topic appears below|Seul le sujet choisi apparaît ensuite/i);
    assert.match(html, /id=["']case-library["']/i);
    assert.match(html, /Open one case, then compare deliberately|Ouvrez un cas, puis comparez avec méthode/i);
    assert.match(html, /Previous topic|Sujet précédent/i);
    assert.match(html, /Next topic|Sujet suivant/i);
    assert.match(html, /id=["']solo-case["']/i);
    assert.match(html, /id=["']maturity-controls["']/i);
    assert.match(html, /id=["']use-patterns["']/i);
    assert.match(html, /Choose the use pattern|Choisissez le mode d’usage/i);
    assert.match(html, /Generation|Génération/i);
    assert.match(html, /Prediction and recommendation|Prédiction et recommandation/i);
    assert.match(html, /Agentic action|Action agentique/i);
    assert.match(html, /Switzerland \+ EU|Suisse \+ UE/i);
    assert.match(html, /Two routes, not one shortcut|Deux routes, aucun raccourci/i);
    assert.match(html, /ai-use-patterns(?:\.fr)?\.md/i);
    assert.match(html, /id=["']non-agentic-cases["']/i);
    assert.match(html, /Same low autonomy, four different evidence contracts|Même avec une faible autonomie, quatre contrats de preuve différents/i);
    assert.match(html, /Read-only field-procedure assistant|Assistant de procédures terrain en lecture seule/i);
    assert.match(html, /Weekly spare-parts demand forecast|Prévision hebdomadaire de pièces/i);
    assert.match(html, /External customer-information chatbot|Chatbot externe d’information client/i);
    assert.match(html, /Product-catalogue accessibility assistant|Assistant d’accessibilité du catalogue/i);
    assert.match(html, /SYNTHETIC EVIDENCE BOUNDARY|FRONTIÈRE DES PREUVES SYNTHÉTIQUES/i);
    assert.match(html, /id=["']control-crosswalk["']/i);
    assert.match(html, /Turn a risk label into controls|Transformez un niveau de risque en contrôles/i);
    assert.match(html, /candidate controls|contrôles candidats/i);
    assert.match(html, /AAP-GOV-001/);
    assert.match(html, /AAP-GOV-003/);
    assert.match(html, /AAP-SEC-002/);
    assert.match(html, /EV-AUDIT-TRACE/);
    assert.match(html, /NIST-AI-100-1/);
    assert.match(html, /href=["']\/data\/control-crosswalk\.v1\.json["']/i);
    assert.match(html, /href=["']\/data\/control-crosswalk\.schema\.json["']/i);
    assert.match(html, /does not claim clause-level equivalence|ne constitue ni une équivalence article par article/i);
    assert.match(html, /href=["']#guided-start["']/i);
    assert.doesNotMatch(html, /property=["']og:image["']|name=["']twitter:image["']/i);
    assert.match(html, /id=["']integration-levels["']/i);
    assert.match(html, /id=["']sectors["']/i);
    assert.match(html, /Healthcare|Santé/);
    assert.match(html, /Education|Éducation/);
    assert.match(html, /Finance/);
    assert.match(html, /Critical infrastructure|Infrastructure critique/);
    assert.match(html, /Non-transferable veto|Condition bloquante/i);
    assert.match(html, /Accessibility assessment|Évaluation d’accessibilité/i);
    assert.match(html, /Fundamental-rights assessment|Analyse des droits fondamentaux/i);
    assert.match(html, /Field-feedback report|Rapport de retour terrain/i);
    assert.match(html, /id=["']field-pilot["']/i);
    assert.match(html, /0\/3/);
    assert.match(html, /Recruiting in Switzerland and the European Union|Recrutement en Suisse et dans l’Union européenne/i);
    assert.match(html, /One bounded A2 business agent|Un agent métier A2 borné/i);
    assert.match(html, /Prepare one real pilot|Préparez un vrai pilote/i);
    assert.match(html, /Nothing entered here is sent|Rien de ce qui est saisi ici n’est transmis/i);
    assert.match(html, /LOW · CENTRAL · HIGH|BASSE · CENTRALE · HAUTE/i);
    assert.match(html, /extrapolation and the observation|L’extrapolation et l’observation/i);
    assert.match(html, /LOCAL DRAFT · INCOMPLETE|BROUILLON LOCAL · INCOMPLET/i);
    assert.match(html, /PUBLICATION REVIEW|REVUE DE PUBLICATION/i);
    assert.match(html, /replaced the demonstration values|remplacé les valeurs de démonstration/i);
    assert.match(html, /Download the local draft|Télécharger le brouillon local/i);
    assert.match(html, /The export always remains a draft|L’export reste toujours un brouillon/i);
    assert.match(html, /id=["']lifecycle-workbench["']/i);
    assert.match(html, /Work through phases 0 to 11|Parcourez les phases 0 à 11/i);
    assert.match(html, /Initial state|Situation initiale/i);
    assert.match(html, /Simplest sufficient system|Système suffisant le plus simple/i);
    assert.match(html, /Security controls|Contrôles de sécurité/i);
    assert.match(html, /Resume this project later|Reprendre ce projet plus tard/i);
    assert.match(html, /Turn answers into records people can operate|Transformer les réponses en documents réellement exploitables/i);
    assert.match(html, /AI system register|Registre du système IA/i);
    assert.match(html, /Implementation checklist|Liste de mise en œuvre/i);
    assert.match(html, /See what changed before yesterday’s decision|Voir ce qui a changé avant qu’une ancienne décision/i);
    assert.match(html, /No reference dossier loaded|Aucun dossier de référence chargé/i);
    assert.match(html, /href=["']\/data\/project-dossier\.schema\.json["']/i);
    if (language === "en") {
      assert.match(html, /templates\/mandate\.md/);
      assert.match(html, /templates\/evaluation-plan\.md/);
      assert.match(html, /templates\/incident-runbook\.md/);
      assert.match(html, /tracks\/en\/independent\.md/);
      assert.match(html, /sectors\/en\/healthcare\.md/);
      assert.match(html, /templates\/accessibility-assessment\.md/);
      assert.match(html, /templates\/fundamental-rights-impact-assessment\.md/);
      assert.match(html, /templates\/field-feedback-report\.md/);
      assert.match(html, /docs\/field-pilot-protocol\.md/);
      assert.match(html, /docs\/field-pilot-cohort\.md/);
      assert.match(html, /examples\/en\/rag-policy-assistant\.md/);
      assert.match(html, /examples\/en\/predictive-demand-forecast\.md/);
      assert.match(html, /examples\/en\/external-customer-chatbot\.md/);
      assert.match(html, /examples\/en\/multimodal-catalog-accessibility\.md/);
      assert.doesNotMatch(html, /templates\/mandate\.fr\.md/);
      assert.doesNotMatch(html, /tracks\/fr\/independent\.md/);
    } else {
      assert.match(html, /templates\/mandate\.fr\.md/);
      assert.match(html, /templates\/evaluation-plan\.fr\.md/);
      assert.match(html, /templates\/incident-runbook\.fr\.md/);
      assert.match(html, /tracks\/fr\/independent\.md/);
      assert.match(html, /sectors\/fr\/healthcare\.md/);
      assert.match(html, /templates\/accessibility-assessment\.fr\.md/);
      assert.match(html, /templates\/fundamental-rights-impact-assessment\.fr\.md/);
      assert.match(html, /templates\/field-feedback-report\.fr\.md/);
      assert.match(html, /docs\/field-pilot-protocol\.fr\.md/);
      assert.match(html, /docs\/field-pilot-cohort\.fr\.md/);
      assert.match(html, /examples\/fr\/assistant-rag-procedures\.md/);
      assert.match(html, /examples\/fr\/prevision-demande-pieces\.md/);
      assert.match(html, /examples\/fr\/chatbot-client-externe\.md/);
      assert.match(html, /examples\/fr\/catalogue-multimodal-accessibilite\.md/);
    }
    assert.doesNotMatch(html, /react-loading-skeleton|Your site is taking shape/i);
  });
}
